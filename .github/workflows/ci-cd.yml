name: CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  packages: write

jobs:
  # ─────────────────────────────────────────────────────────────────────────────
  # CI: Test & Build for all services
  # ─────────────────────────────────────────────────────────────────────────────

  go-services:
    name: Go ${{ matrix.service }} (gofmt/vet/build)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service:
          - auth
          - module
          - analytics
          - control
          - alert
          - audit
          - notification
          - stream
          - export
          - wsgateway
          - dlq
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache-dependency-path: services/${{ matrix.service }}/go.sum

      - name: gofmt check
        run: |
          cd services/${{ matrix.service }}
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "::error::The following files are not gofmt-clean:"
            echo "$unformatted"
            exit 1
          fi

      - name: go vet
        run: cd services/${{ matrix.service }} && go vet ./...

      - name: go build
        run: cd services/${{ matrix.service }} && go build ./...

  ml-service:
    name: ML Service (pytest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/ml
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest httpx
      - name: pytest
        run: pytest -q || true

  docker-build:
    name: Docker build & push ${{ matrix.service }}
    runs-on: ubuntu-latest
    needs: [go-services, ml-service]
    strategy:
      fail-fast: false
      matrix:
        service:
          - auth
          - module
          - analytics
          - control
          - alert
          - audit
          - notification
          - stream
          - export
          - wsgateway
          - dlq
          - ml
          - mysqld-exporter-all
          - postgres-exporter-all
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        run: |
          IMAGE=ghcr.io/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')/${{ matrix.service }}
          if [[ "${{ matrix.service }}" == "mysqld-exporter-all" || "${{ matrix.service }}" == "postgres-exporter-all" ]]; then
            CONTEXT=infra/${{ matrix.service }}
            DOCKERFILE=infra/${{ matrix.service }}/Dockerfile
          else
            CONTEXT=services/${{ matrix.service }}
            DOCKERFILE=services/${{ matrix.service }}/Dockerfile
          fi
          docker buildx build -f "$DOCKERFILE" "$CONTEXT" \
            --cache-from type=gha \
            --cache-to type=gha,mode=max \
            --tag $IMAGE:latest \
            --tag $IMAGE:${{ github.sha }} \
            --push

  dashboard-docker-build:
    name: Dashboard Docker build & push
    runs-on: ubuntu-latest
    needs: dashboard
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Dashboard image
        run: |
          IMAGE=ghcr.io/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')/dashboard
          docker buildx build -f dashboard/Dockerfile dashboard \
            --cache-from type=gha \
            --cache-to type=gha,mode=max \
            --tag $IMAGE:latest \
            --tag $IMAGE:${{ github.sha }} \
            --push

  dashboard:
    name: Dashboard (lint/build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dashboard
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: dashboard/package-lock.json
      - name: npm ci
        run: npm ci
      - name: lint
        run: npm run lint
      - name: build
        run: npm run build

  # ─────────────────────────────────────────────────────────────────────────────
  # CD: Deploy to Self-Hosted Server (only on push to main)
  # ─────────────────────────────────────────────────────────────────────────────

  cd-deploy:
    name: Deploy to Server
    needs: [go-services, ml-service, docker-build, dashboard-docker-build]
    if: github.ref == 'refs/heads/main'
    runs-on: self-hosted

    steps:
      - name: Pre-checkout Fix Permissions
        run: |
          echo "Fixing workspace ownership before checkout..."
          sudo chown -R $(whoami):$(whoami) $GITHUB_WORKSPACE 2>/dev/null || true
          sudo rm -rf $GITHUB_WORKSPACE/dashboard/node_modules 2>/dev/null || true
          echo "Workspace permissions fixed."

      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          clean: false
          fetch-depth: 1
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            docker-compose.yml
            .env.example
            infra

      - name: Configure Runner Network Retries
        run: |
          npm config set fetch-retries 5
          npm config set fetch-retry-mintimeout 20000
          npm config set fetch-retry-maxtimeout 120000

      - name: Log in to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u $(echo '${{ github.actor }}' | tr '[:upper:]' '[:lower:]') --password-stdin

      - name: Set up Docker Compose Environment
        run: |
          echo "Preparing .env from GitHub Secrets..."
          cp .env.example .env
          echo "MYSQL_ROOT_PASSWORD=${{ secrets.MYSQL_ROOT_PASSWORD }}" >> .env
          echo "DB_USER=${{ secrets.DB_USER }}" >> .env
          echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}" >> .env
          echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> .env
          echo "CLOUDFLARED_TUNNEL_TOKEN=${{ secrets.CLOUDFLARED_TUNNEL_TOKEN }}" >> .env
          echo "MINIO_ACCESS_KEY=${{ secrets.MINIO_ACCESS_KEY }}" >> .env
          echo "MINIO_SECRET_KEY=${{ secrets.MINIO_SECRET_KEY }}" >> .env
          echo "ADMIN_PASSWORD=${{ secrets.ADMIN_PASSWORD }}" >> .env
          echo "KONG_JWT_SECRET_FRONTEND=${{ secrets.KONG_JWT_SECRET_FRONTEND }}" >> .env
          echo "KONG_JWT_SECRET_ESP32=${{ secrets.KONG_JWT_SECRET_ESP32 }}" >> .env
          echo "MODULE_NATS_PASSWORD=${{ secrets.MODULE_NATS_PASSWORD }}" >> .env
          echo "ALERT_NATS_PASSWORD=${{ secrets.ALERT_NATS_PASSWORD }}" >> .env
          echo "CONTROL_NATS_PASSWORD=${{ secrets.CONTROL_NATS_PASSWORD }}" >> .env
          echo "AUTH_NATS_PASSWORD=${{ secrets.AUTH_NATS_PASSWORD }}" >> .env
          echo "ANALYTICS_NATS_PASSWORD=${{ secrets.ANALYTICS_NATS_PASSWORD }}" >> .env
          echo "NOTIFICATION_NATS_PASSWORD=${{ secrets.NOTIFICATION_NATS_PASSWORD }}" >> .env
          echo "AUDIT_NATS_PASSWORD=${{ secrets.AUDIT_NATS_PASSWORD }}" >> .env
          echo "WS_NATS_PASSWORD=${{ secrets.WS_NATS_PASSWORD }}" >> .env
          echo "CCTV_ML_API_TOKEN=${{ secrets.CCTV_ML_API_TOKEN }}" >> .env
          echo "CCTV_AUTH_USERNAME=${{ secrets.CCTV_AUTH_USERNAME }}" >> .env
          echo "CCTV_AUTH_PASSWORD=${{ secrets.CCTV_AUTH_PASSWORD }}" >> .env
          echo "GRAFANA_ADMIN_PASSWORD=${{ secrets.GRAFANA_ADMIN_PASSWORD }}" >> .env
          echo "REDIS_PASSWORD=${{ secrets.REDIS_PASSWORD }}" >> .env

      - name: Fix Volume Permissions
        run: |
          echo "Fixing Docker volume permissions..."
          # Grafana runs as UID 472 inside container
          sudo chown -R 472:472 ./volumes/grafana 2>/dev/null || true
          # MinIO runs as UID 1000 inside container
          sudo chown -R 1000:1000 ./volumes/minio 2>/dev/null || true
          # MariaDB/MySQL directories
          sudo chown -R 999:999 ./volumes/mariadb-* 2>/dev/null || true
          # TimescaleDB/PostgreSQL directories
          sudo chown -R 70:70 ./volumes/timescaledb-* 2>/dev/null || true
          # Prometheus runs as UID 65534 (nobody:nogroup) inside container
          sudo chown -R 65534:65534 ./volumes/prometheus 2>/dev/null || true
          sudo chmod -R 777 ./volumes/prometheus 2>/dev/null || true
          # Redis, cAdvisor, node-exporter
          sudo chown -R $(whoami):$(whoami) ./volumes/redis-shared ./volumes/cadvisor ./volumes/node-exporter 2>/dev/null || true
          echo "Volume permissions fixed."

      - name: Force Clean Workspace
        run: |
          echo "Stopping existing containers..."
          docker compose down --remove-orphans || true
          echo "Removing old images..."
          docker image prune -f || true

      - name: Pull and Deploy with Docker Compose
        run: |
          if docker compose pull; then
            echo "Pull succeeded, deploying without rebuild"
            docker compose up -d --no-build --remove-orphans
          else
            echo "Pull failed, falling back to local build for missing images"
            docker compose up -d --build --remove-orphans
          fi

      - name: Clean Up Old Docker Images
        if: always()
        run: |
          docker image prune -f
