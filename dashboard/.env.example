# Dashboard environment
# Base URL of the Kong API Gateway (single entry point to the backend).
# The auth feature calls Kong at this URL (/auth/*). Kong routes to the Auth Service.
VITE_API_URL=http://localhost:8000
